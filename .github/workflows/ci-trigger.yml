name: CI

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    name: CI - lint / typecheck / test
    runs-on: ubuntu-latest
    steps:
      - uses: 0xPolygon/pipelines/.github/actions/ci@main
